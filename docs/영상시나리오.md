# 시연영상 시나리오 — 3분 (180초)

> 대상: 캡컷 편집용. 이동 중 휴대폰에서 이 문서만 보고 컷을 붙일 수 있게 썼다.
>
> **제출 방법: 유튜브 업로드 후 URL 기재. 별도 파일 제출 불가** (공식 서식).
> 업로드까지가 작업이다.
>
> 원칙 셋
> 1. **자막만으로 완결된다.** 심사위원이 무음으로 볼 수 있다. 오디오는 나중 선택지.
> 2. **배속 구간은 화면에 배속을 상시 표기.** 표기 없는 배속은 조작으로 읽힌다.
> 3. **180초를 넘기지 않는다.** 규정이다. 넘치면 §5 순서를 따른다.

---

## 0. 이 영상이 답해야 하는 질문

심사에서 지적받은 것: **"공개 설계라서 차별화가 가장 안 된다."**

그리고 조사에서 확인한 것: 이 대회에서 하드웨어 출품작이 대상까지 간 경로는
**재현성**이었다(2020년 학생부문 대상 인정 사유가 *"매뉴얼 + 명령어 하나로 자동 구축"*).
심사위원은 우리 로봇을 만질 수 없다.

그러니 영상 전체가 이 두 문장에 답해야 한다.

> **공개 시뮬레이션은 실물을 예측하지 못한다.** 거기서 걷는다고 실물이 걷지 않는다.
> **우리는 로봇 없이도 확인할 수 있는 형태로 만들었다.**

"우리도 만들었다"가 아니라 **"우리는 왜 안 되는지 알아냈고, 남이 확인할 수 있게 했다"**.

---

## 1. 컷 시트

`▶` 실물 영상 · `▣` 정지 이미지 · `▤` 화면 녹화 · `◈` 시뮬레이션 렌더(CTO 제공)

| # | 시각 | 길이 | 화면 | 자막 |
|---|---|---|---|---|
| **1** | 0:00–0:07 | 7s | ▶ 실물 보행 측면, **무편집 등속** | **오픈소스 4족보행 로봇** |
| **2** | 0:07–0:15 | 8s | ▶ 같은 컷, 발끝으로 서서히 줌인 | 부품비 **459,000원** |
| **3** | 0:15–0:23 | 8s | ▣ `Images/` 업스트림 로봇 GIF | 원작자의 로봇은 **걷는다** |
| **4** | 0:23–0:32 | 9s | ▶ **실패 — 주저앉음/넘어짐** | 그런데 우리 로봇은<br>**걷지 않았다** |
| **5** | 0:32–0:40 | 8s | ▣ 계보 + 서보 4종 가격 (§3-A) | 손댄 사람이 여럿이고<br>**각자 다른 부품을 썼다** |
| **6** | 0:40–0:52 | 12s | ◈ **`cut1.mp4`** 두 로봇 나란히 보행 | 같은 보행 코드<br>**다른 치수** |
| **7** | 0:52–1:02 | 10s | ▤ 코드 3개 파일 나란히 스크롤 | 치수가 코드 **세 곳**에<br>흩어져 있었다 |
| **8** | 1:02–1:08 | 6s | ▣ 2020년 커밋 메시지 전문 | 왜 바뀌었는지<br>**어디에도 없다** |
| **9** | 1:08–1:16 | 8s | ▶ 캘리퍼스로 다리 실측 | 그래서 **직접 쟀다** |
| **10** | 1:16–1:26 | 10s | ▣ 파이프라인 도식 (§3-B) | 실측값 **한 곳**에서<br>모델을 **생성**한다 |
| **11** | 1:26–1:36 | 10s | ◈ **`cut2.mp4`** 좌우 비교 | 어긋나면 **검증이 잡는다** |
| **12** | 1:36–1:44 | 8s | ▤ `git clone` → `make verify` | 로봇도 GPU 도<br>**필요 없다** |
| **13** | 1:44–1:52 | 8s | ▤ `8/8 통과`, `0.0000mm` 정지 | 검증 **8단계**<br>기하 **0.0000mm** |
| **14** | 1:52–2:01 | 9s | ▤ `make eval` → 정책이 걷는 화면 | 학습된 정책을<br>**그대로 재생한다** |
| **15** | 2:01–2:10 | 9s | ▣ 좌: 업스트림 적층 / 우: 우리 전면 | 부품 배치도<br>다시 설계했다 |
| **16** | 2:10–2:19 | 9s | ▣ 전면 → 배면 전환 | 전면 제어부 / 배면 전원부<br>**양면 분리** |
| **17** | 2:19–2:29 | 10s | ◈ `cut3.mp4` + ▶ 실물 **좌우 분할** | 시뮬레이션과 실물을<br>**같은 자로** |
| **18** | 2:29–2:41 | 12s | ▣ 3층 일치 표, 한 줄씩 (§3-C) | 기하 **0.0000mm**<br>각속도 **2%** · 속도 **4%** |
| **19** | 2:41–2:56 | 15s | ◈ RL 보행 + 슬루 수치 **[2×]** | 강화학습은 **3.4배 빨랐다**<br>그런데 서보 정격의 **178%**<br>**올리기 전에 걸러냈다** |
| **20** | 2:56–3:00 | 4s | ▶ 정면 보행 + 저장소 주소 | **KINETIQ**<br>github.com/robertchoi/oss_spotmicro |

**합계 180초 정확.**

---

## 2. 컷별 편집 메모

### 1–2번 (훅)
**첫 10초로 계속 볼지 정해진다.** 로고·타이틀 카드로 시작하지 말 것. 첫 프레임부터
로봇이 걷고 있어야 한다. **여기는 반드시 등속** — 시작부터 배속이면 나머지를 다 의심한다.

### 3–5번 (문제) ← **원작자를 탓하지 않는다**
- 3번이 중요하다. **"원작자의 로봇은 걷는다"** 를 먼저 인정하고 시작한다. 심사위원이
  원작 README 를 열어도 모순이 없다 — 거기 로봇이 걷는 건 우리 주장의 **전제**다.
- 4번 **실패 영상이 이 영상의 앵커**다. 실패를 보여주는 출품작은 드물고, 6~8번 진단이
  여기서 설득력을 얻는다. 없으면 8/25 촬영 1순위.
- 5번 자막은 "부품도 그대로 샀다"가 **아니다.** 우리도 서보를 바꿨다. 계보와 가격만 보인다.

### 6번 (`cut1.mp4`) ← **차별화의 핵심**
같은 보행 코드에 **상수만 다르게** 물려 두 로봇을 나란히 걷게 한 화면이다. 표로
설명할 것을 움직임이 대신한다. **12초를 아끼지 말 것.** 좌우 어느 쪽이 상속값인지
작게 라벨을 붙이면 좋다(`상속받은 치수` / `실측 치수`).

### 7–8번 (진단)
- 7번은 세 파일이 **동시에 뜨면 안 된다.** 한 파일씩 3초 간격으로.
- 8번은 커밋 메시지 전문을 그대로 보여준다. *"Folder ordered, Useless Images deleted,
  keyborad controller done"* — 다리 길이를 바꾼 커밋인데 그 얘기가 없다는 것이 요지다.

### 11번 (`cut2.mp4`)
순기구학 대조 게이트가 무엇을 잡는지 보이는 화면이다. **좌: 빨간 마커가 발에서
50mm 벗어남 / 우: 마커가 발을 감쌈.** 자막 없이도 읽히지만, 좌우에 작은 라벨을
붙이면 확실하다. 마커가 벗어난 쪽에 화살표를 하나 넣어도 좋다.

### 12–14번 (재현성 블록) ← **이 대회의 승부처**
- 심사위원이 **"내가 돌릴 수 있다"** 를 눈으로 확인하는 25초다. 10월 2차 기능테스트에
  실제로 이 경로가 실행된다.
- **터미널 글자가 작으면 무의미하다.** 폰트를 키워 녹화하고, `8/8 통과` 와 `0.0000mm`
  에서 각각 1.5초 정지.
- 14번은 `make eval` 이 정책을 재생하는 화면. **명령은 `0.3` 을 써야 걷는다**
  (`make eval` 기본값이 그렇게 맞춰져 있다).

### 15–16번 (하드웨어)
**좌우 대비가 전부다.** 업스트림 사진(부품이 몸통 안에 적층)과 우리 사진(전면에 정렬)을
같은 크기로 놓는다. 심사에서 지적받은 "공개 설계라 차별화가 안 된다"에 대한 직접 반박이다.

### 17–18번 (일치)
- 17번 분할은 **보폭 위상을 맞춰야** 의미가 있다. 안 맞으면 위아래 순차 컷으로 바꾼다.
- 18번 `0.0000` 은 **소수점 네 자리를 그대로** 보여준다. `0mm` 로 줄이면 반올림으로 읽힌다.
- 속도가 **4%** 다(시뮬 52 vs 실물 49~50). 2% 가 아니다 — 이건 새로 생긴 **동역학** 층위다.

### 19번 (강화학습) ← **정직함이 강점이 되는 컷**
- 자랑이 아니라 **안전 장치가 일한 사례**로 보여준다. 정책이 하드웨어가 못 내는 명령으로
  속도를 얻었고, 우리 평가 게이트가 실물에 올리기 전에 걸러냈다.
- 화면에 `972°/s (정격 545 의 178%)` 와 **⚠ 실물 이식 불가** 를 띄운다.
  `make eval` 출력을 그대로 녹화하면 된다.
- 배속 표기 `2×` 필수. **실물에 올린 것처럼 보이는 편집(실물 컷과 교차) 금지.**

### 20번 (마무리)
저장소 주소는 **4초 내내** 화면에. 마지막 프레임은 로봇이 걷는 장면으로 끝낸다.

---

## 3. 만들어 넣을 그래픽 3종

캡컷 텍스트만으로 된다.

### A. 5번 — 계보와 부품

```
        김덕연 (원설계)      MG996R      3,200원
        FlorianWilk
        한양대 Road Balance  CLS6336HV  49,000원
        robertchoi
        orocapangyo
        KINETIQ            DS3235      ← 우리
```

> 여유가 있으면 한 줄 덧붙인다 — **"한양대 구성은 서보 12개만 588,000원.
> 우리 로봇 전체가 459,000원."**

### B. 10번 — 파이프라인

```
   실측값 한 곳
        │
        ├──→  역기구학 (실물 제어)
        └──→  시뮬레이션 모델 자동 생성
                    │
                 검증 8단계
```

**화살표가 한 점에서 갈라지는 것**이 요지다. 왼쪽에 이전 구조(세 곳에 따로)를 흐리게
두고 대비시키면 산다.

### C. 18번 — 3층 일치

```
  기하 (정적)        0.0000 mm      완전 일치
  무릎 각속도        323 vs 318      2%
  보행 속도 (동역학)  52 vs 49~50     4%
```

세 번째 줄이 새로 생긴 층위다. **실물에서 걷는 궤적을 시뮬레이터에 그대로 재생해서
같은 속도가 나오는지 잰 것** — 기하가 아니라 질량·관성·마찰까지 맞다는 뜻이다.

---

## 4. 아직 없는 소재 — 8/25(화) 촬영 목록

우선순위 순. **위 셋만 찍어도 영상은 완성된다.**

| 순위 | 무엇 | 컷 | 길이 | 메모 |
|---|---|---|---|---|
| **1** | **실패 영상** — 주저앉음/넘어짐 | 4 | 3~5s | 영상의 앵커. 없으면 6~8번이 붕 뜬다 |
| **2** | 캘리퍼스 실측 클로즈업 | 9 | 5s | 다리를 무는 순간. 손 흔들림 주의 |
| **3** | 1m + 스톱워치 보행 | (예비) | 20s | 재측정 겸용. 두 번 찍는다 |
| 4 | 정면 보행 | 20 | 8s | 카메라 정면으로 걸어옴 |
| 5 | 터미널 녹화 3종 (`make verify` / `make eval`) | 12–14, 19 | 25s | **노트북에서. 폰트 크게** |

**CTO 제공 완료** — `docs/media/cut1.mp4` `cut2.mp4` `cut3.mp4` (1920×1080 / 30fps,
화면에 글자 없음. 자막은 편집에서 얹는다).

---

## 5. 시간이 넘칠 때 잘라낼 순서

편집하면 반드시 넘친다. **이 순서로만 자른다.**

| 순서 | 자를 것 | 회수 |
|---|---|---|
| 1 | 2번 후반 (부품비) | 4s |
| 2 | 16번 (전면/배면 전환) | 4s |
| 3 | 17번 (분할 화면) | 5s |
| 4 | 8번 (커밋 메시지) | 3s |

**절대 자르지 않는 것: 4번(실패), 6번(`cut1` 두 로봇), 12~14번(재현성), 18번(3층 일치).**
이 넷이 없으면 다른 SpotMicro 영상과 구별되지 않는다.

---

## 6. 자막 스타일

| 항목 | 값 | 이유 |
|---|---|---|
| 위치 | 하단, 아래에서 15% | 폰·유튜브 UI 에 안 가림 |
| 줄 수 | **최대 2줄** (19번만 3줄) | 3줄은 읽다가 컷이 넘어간다 |
| 한 줄 | 한글 **16자 이내** | |
| 글꼴 | 굵은 고딕 | 무료 폰트 무방 |
| 색 | 흰 글씨 + **검은 외곽선/반투명 띠** | 밝은 바닥에서 흰 글씨가 사라진다 |
| 숫자 | 노란색 또는 더 굵게 | 표 안 숫자도 동일 |
| 등장 | 컷 시작 0.3초 후, 끝 0.3초 전 퇴장 | 동시에 뜨면 깜빡임으로 보인다 |

**배속 표기:** 우상단 `2×`, 흰 글씨 검은 띠, **해당 컷 전체 길이 동안 상시.**

---

## 7. 오디오

**1차 편집에서는 넣지 않는다.** 자막만으로 완결된 3분을 먼저 만든다.

남으면 이 순서로: ① 배경음악만(-20dB) → ② 실물 컷의 **서보 원음**을 1·4·20번에 살짝
(로봇이 실제로 움직인다는 증거가 된다) → ③ 나레이션은 마지막.

---

## 8. 제출 전 점검

- [ ] **180초 이하**인가 (179초 권장)
- [ ] **유튜브 업로드 완료**, URL 확보 (미등록/비공개 아님 확인)
- [ ] **음소거로 한 번 재생**해서 내용이 전달되는가
- [ ] 배속 컷에 배속 표기가 **컷 전체**에 있는가
- [ ] 업스트림 사진에 **출처 표기** (CC BY 3.0)
- [ ] 강화학습이 **실물에서 완료된 것처럼** 보이지 않는가 (19번)
- [ ] 저장소 주소 오타 — `github.com/robertchoi/oss_spotmicro`
- [ ] **노트북 화면**에서 한 번 볼 것 (자막 크기)
