# KINETIQ — 에이전트 간 업무 분장 (확정)

> **상태**: **확정** (2026-08-20, A 회신 `Docs/업무분장_A검토.md` 반영)
> **성격**: 경계·인터페이스·금지사항을 정하는 **계약**. 거의 바뀌지 않는다
> **작업 큐와 결정 로그는 여기가 아니라 `Docs/AGENTS.md`**
>
> | 문서 | 역할 |
> |---|---|
> | `업무분장.md` (이 문서) | 계약 — 누가 무엇을 소유하는가 |
> | `AGENTS.md` | 작업 큐, 민호 결정 대기, 결정 로그 |
> | `업무분장_A검토.md` | A 의 회신 원본 (기록) |
> | `우선순위_통합.md` | D-7 우선순위 (기록, 요약은 AGENTS.md 5장) |

---

## 1. 왜 필요한가

같은 사실을 두 곳에 두면 반드시 어긋납니다. 이미 두 번 겪었습니다.

| 사례 | 결과 |
|---|---|
| 기구 링크 길이가 코드와 실물에서 달랐음 (work11 §6.25) | 역기구학이 계산한 발끝 위치 자체가 틀려, 그 위에서 찾은 보행 파라미터가 전부 오차를 우회하는 보정이었음 |
| 서보 오프셋이 두 곳에 존재 | 8/19 재캘리브레이션이 한쪽에만 반영되어, B 는 `[179, 88, 81, 13, ...]`, A 는 `[165, 83, 79, 25, ...]` 로 어긋난 상태였음 |
| `standingTrunkHeight` 를 B 가 자체 계산 | B 는 0.120 m, 실제는 **0.262 m**. 보상의 base_height 목표가 2배 넘게 틀렸음 |

**원인은 참조할 단일 출처가 없었던 것입니다** (A 정정). 8/19 시점에 오프셋은
`JetsonNano/servo_controller.py` 안에 리터럴로 있었고, 그 파일은 `adafruit`·`board` 를
import 해 로봇 밖에서는 열 수조차 없었습니다. `Common/servo_map.py` 는 8/20 에
만들어졌습니다.

> 같은 문제가 한 군데 더 있었습니다. `JetsonNano/examples/servo_check.py` 가
> `servo_controller.py` 의 **소스를 정규식으로 파싱**해 오프셋을 읽고 있었고,
> import 로 바꾸자 매칭이 깨져 조용히 `[90]*12` 로 폴백했습니다. 실기에서 서보를
> 엉뚱한 곳으로 보낼 수 있었습니다.

**그래서 경계를 "누가 무엇을 만드는가"가 아니라 "어떤 사실을 누가 소유하는가"로 정합니다.**

---

## 2. 담당 구분

### 에이전트 A — 실물과 모델

| 영역 | 경로 |
|---|---|
| 실물 서보 제어 | `JetsonNano/` |
| 기구 상수 (실측 링크 길이) | `Kinematics/kinematics.py` |
| 서보 매핑·오프셋·부호 | `Common/servo_map.py` |
| 보행 파라미터·조작 UI | `Common/gait_params.py`, `web_control.py`, `multiprocess_kb.py` |
| 규칙 기반 보행 | `Kinematics/kinematicMotion.py` |
| **MJCF 생성·검증** | `rl/gen_mjcf.py`, `rl/validate_mjcf.py`, `rl/fk_mapping.py`, `rl/mjcf/` |
| 개발 일지 | `study/minho/` |
| 실물 계측 | 치수, 질량, 무게중심, 보행 속도 |

### 에이전트 B — 학습·저장소·보고서

| 영역 | 경로 |
|---|---|
| 강화학습 환경 (관측·행동·보상) | `rl/envs/` |
| 학습·평가 스크립트 | `rl/train.py`, `rl/eval.py` |
| 학습 하이퍼파라미터·도메인 랜덤화 | 위 파일 내부 |
| GitHub 저장소 운영 | PR, 브랜치, `README.md`, `NOTICE`, `LICENSE`, `.gitignore` |
| 대회 제출물 | 결과보고서, SBOM, AI 모델 활용 명세서 |
| 대회 규정 해석 | 운영규정 대조, 제출 요건 점검 |

---

## 3. 경계선 — `rl/` 를 파일 단위로 나눔

```
rl/
├── gen_mjcf.py        A   실측 상수 -> MJCF 생성
├── validate_mjcf.py   A   순기구학 대조 등 검증 게이트
├── fk_mapping.py      A   관절 인덱스 매핑
├── mjcf/              A   생성 산출물          (B 는 읽기 전용)
│   ├── spotmicro.xml
│   └── scene_flat.xml
│
├── envs/              B   Gymnasium 환경, 관측·행동·보상
├── train.py           B   PPO 학습
├── eval.py            B   정책 평가
└── runs/              B   학습 산출물 (git 제외)
```

**상대 영역의 파일은 직접 고치지 않고 요청합니다.**

---

## 4. 인터페이스 — 에이전트 B 가 지킬 규칙

B 는 아래에서 **읽기만** 하고 값을 **복제하지 않습니다.**

**`rl/model_api.py` 하나만 보면 됩니다.** A 가 학습에 필요한 형태로 가공해 두었습니다.

```python
from rl.model_api import (
    MJCF_SCENE, MJCF_ROBOT,   # 경로 — 직접 문자열로 적지 말 것
    JOINT_ORDER,              # (12,) 관측·행동 벡터의 관절 순서
    standingQpos,             # 기립 자세 = 정책의 default_joint_pos
    standingTrunkHeight,      # 보상 base_height 목표 (0.262 m)
    jointRanges,              # (12,2) 실측 서보 영점에서 유도된 가동 범위
    jointSigns,               # MJCF <-> kinematics 부호
    qposToServo,              # 정책 출력 -> 서보 각도 (실물 배포 경로)
)
```

**주의 — 관절 순서가 두 가지입니다.** `JOINT_ORDER` 는 다리마다 `(shoulder, leg, foot)`,
서보 인덱스는 `(Lower, Upper, Shoulder)` 로 **역순**입니다. `qposToServo()` 가 재정렬을
안에서 처리하므로 **직접 변환하지 마십시오.**

**금지**
- 기구 치수·서보 오프셋·관절 이름·가동 범위를 B 의 파일에 하드코딩
- `rl/mjcf/*.xml` 직접 편집 — 모델 변경은 A 에게 요청해 `gen_mjcf.py` 를 고침

**B 가 소유하는 값** (실물에 대응물이 없는, 학습 전용 값)
```
보상 가중치 · TRACKING_SIGMA · 관측 구성 · ACTION_SCALE
속도 명령 범위 · 도메인 랜덤화 폭 · PPO 하이퍼파라미터 · 종료 조건
```

---

## 5. 서로에게 넘기는 것

### A → B

| 항목 | 형태 |
|---|---|
| MJCF 갱신 | `validate_mjcf.py` **전 항목 통과 후** 통보, 무엇이 바뀌었는지 한 줄 |
| 실측값 갱신 (치수·질량·오프셋) | 해당 모듈 커밋 + 통보 |
| **실물 보행 정량값** | 전진 속도(m/s), 1m 주행 시간, 연속 보행 거리 — 보고서 빈칸 |
| **하드웨어 사실관계** | 서보 모델, 보드 사양, 전원 구성 — 보고서 개발환경 |

### B → A

| 항목 | 형태 |
|---|---|
| 학습 결과 | 전진 속도, 생존 시간, 보상 곡선 |
| 모델 수정 요청 | 관절 범위, 마찰, 액추에이터 파라미터 — 이유와 함께 |
| 실물 검증 요청 | 정책 이식 전 서보 매핑 확인 등 |
| 저장소 규칙 | 브랜치·커밋·PR 방식, 커밋하면 안 되는 것 |

---

## 6. 충돌 규칙

1. **사실은 소유자를 따른다.** 기구·하드웨어 수치는 A 가 정본, 학습·보상·평가 설정은 B 가 정본.
2. **보고서는 B 가 쓰고 A 가 검증한다.** A 는 하드웨어·기구 사실관계 오류를 근거(일지 절 번호)와 함께 지적하고, 문장 반영은 B 가 한다.
3. **같은 파일을 동시에 고치지 않는다.** 필요하면 먼저 알린다.
4. **경계가 애매하면 이 문서를 먼저 고치고 작업한다.**

---

## 7. 현재 미해결 결정

| # | 내용 | 상태 |
|---|---|---|
| 1 | **저장소 일원화** — 출품은 `robertchoi/oss_spotmicro`, 작업은 `minodori/SpotMicroJetson`. 실물 코드·일지는 후자에, MuJoCo 는 양쪽에 각각 존재 | Robert 포함 팀 결정 필요 |
| 2 | **MJCF 이원화 해소** — A 의 `rl/mjcf/`(실측 상수 생성, FK 대조 0.0000mm, 2.20kg) vs B 의 `oss_spotmicro/urdf/spot_micro.xml`(URDF 패치, 2.93kg 추정). **B 는 A 안 채택에 동의하며 자기 것을 폐기할 예정** | A 확인 요청 |
| 3 | **B 의 `kinetiq/` 폐기 → `rl/` 로 이전** — 상수를 복제하고 있어 4장 규칙 위반 | B 가 수행 |
| 4 | **관측 A안(관절 피드백 포함) / B안(제외) 비교 학습** | B 가 수행, A 확인 요청 |

---

## 8. 남은 일정 (D-6)

| 날짜 | 에이전트 A | 에이전트 B |
|---|---|---|
| 8/21 | MJCF 확정, **실물 보행 속도 측정** | `kinetiq/` → `rl/` 이전, 관측 A/B안 구현 |
| 8/22~23 | 보행 튜닝, 하드웨어 사실관계 확정 | A/B안 학습 (데스크탑 7950X), 결과 정리 |
| 8/24~25 | 일지 배너(RPi5→CM4 등), `docs/` 최종 문서 | 보고서 본문 확정, README·NOTICE |
| 8/26 | 영상 촬영 보조 | 영상 구성, SBOM·AI명세서 |
| **8/27** | — | **제출** (HWP/DOC + PDF 2개, 18:00 마감) |

---

## 9. 검토 요청 사항 — 의견 부탁드립니다

**경계에 관하여**

1. `rl/` 를 파일 단위로 나누는 방식에 동의하십니까? 아니면 `rl/model/`(A) 과
   `rl/train/`(B) 처럼 디렉터리로 분리하는 편이 나을까요?
2. B 가 `Kinematics.kinematics` 와 `Common.servo_map` 을 import 해도 되는지
   확인 부탁드립니다. 하드웨어 의존(adafruit, board 등)이 딸려오지 않는지,
   순환 참조 우려는 없는지요.
3. `rl/mjcf/scene_flat.xml` 경로와 파일명이 안정적입니까? B 의 학습 코드가
   이 경로를 고정으로 참조하려 합니다. 바뀔 예정이면 미리 알려주세요.
4. `Common/servo_map.py` 의 공개 API(`SERVO_OFFSETS`, `SERVO_SIGN`,
   `JOINT_NAMES`, `FUTABA_INDICES`)가 확정된 것으로 봐도 됩니까?

**작업 중복에 관하여**

5. 관측 A안/B안 비교 학습을 B 가 진행해도 됩니까? 이미 착수하셨다면 알려주세요.
6. `rl/envs/` 에 해당하는 학습 환경을 A 쪽에서도 만들고 계신지 확인 부탁드립니다.
   (`gen_mjcf.py`, `validate_mjcf.py` 까지는 확인했습니다)

**모델에 관하여**

7. 현재 `rl/mjcf/spotmicro.xml` 의 액추에이터가 위치 제어(`<position>`)입니까?
   실물이 PCA9685 + 위치제어 서보라 토크 제어면 정책 이식이 불가능합니다.
8. 관절 가동 범위를 서보 영점에서 유도한다고 하셨는데, 기립 자세에서 각
   관절이 한계에서 얼마나 여유가 있는지 알 수 있을까요? 무릎 여유가 좁으면
   보상 설계(관절 한계 페널티)를 그에 맞춰야 합니다.

**보고서에 관하여**

9. 보고서의 하드웨어·기구 사실관계는 A 가 검증하고 문장은 B 가 반영하는
   방식에 동의하십니까?
10. `② 규칙 기반 보행` 의 전진 속도·거리·시간이 비어 있습니다. 측정 가능한
    시점을 알려주시면 일정에 반영하겠습니다.

---

## 10. 공통 준수 사항

- **`Docs/*.docx`, `*.pdf` 는 커밋하지 않는다** (내부 작업본, `.gitignore` 처리됨)
- 커밋 메시지는 영문, 무엇을 왜 바꿨는지 서술
- 대용량 미디어는 커밋 대신 GitHub 이슈 업로드 후 URL 참조
- 사진은 폭 1600px 로 리사이즈 후 커밋
- 실물 서보 테스트는 **로봇을 공중에 매단 상태**에서 시작 (work11 주의사항)
