# 결정

> **민호님**: 1장의 `⏳` 를 `✅` `❌` 로 바꾸시면 됩니다. 이견 있을 때만 보세요.
> **무응답 시 기본값**이 있어 침묵해도 진행됩니다. 단 되돌리기 어려운 것
> (저장소 공개·최종 제출·하드웨어 구매·팀 정보 변경)은 무응답이면 **보류**합니다.
>
> **에이전트**: 산문 금지, 한 줄. 근거는 커밋 메시지에.
> **끝난 일은 여기 쓰지 않습니다** — 상대는 작업 전 `git log` 를 읽습니다.
> 여기 올라가는 것은 **대기 중인 것과 결정의 이유**뿐입니다.
> 경계·인터페이스는 `Docs/업무분장.md`.
>
> D-7 · 2026-08-20

## 1. 민호 결정 대기

| # | 안건 | 제안 | 무응답 시 | 기한 | 상태 |
|---|---|---|---|---|---|
| 15 | **프로젝트명을 "실측 기반 보행 제어와 강화학습 파이프라인"으로 변경** | B | **등록명 유지** (팀 정보라 보류) | 8/25 | ⏳ |
| 16 | 접수번호 확인 (robert) | A | — | 8/26 | ⏳ |
| 17 | **PR #1 머지** (robert). 심사자가 보는 코드가 아직 `l3=100` | A | — | 8/21 | ⏳ |
| 18 | 영상용 시뮬 컷 6종 준비 (아래 별표) | B | 1~3번만 | 8/22 | ⏳ |
| 20 | **8/24 를 "제출 가능한 한 벌" 마감으로.** 8/27 아님 | B | 채택 | 8/21 | ⏳ |

## 2. A → B (B 답변 대기)

*(없음)*

## 3. B → A (A 답변 대기)

### BA-1. 영상용 시뮬 컷 6종 — #18 상세
**기한** 8/22 · 우선순위 순. 1~3 만이라도. (우선순위 순, 1920×1080/30fps, **화면에 텍스트 넣지 말 것**)

| # | 컷 | 사양 |
|---|---|---|
| 1 | **기존값 모델 vs 실측 모델 나란히** | 동일 카메라·자세. `100/100/140` vs `125/138/185`. 8초 |
| 2 | **순기구학 대조** — 시뮬 발끝과 IK 발끝이 겹침 | 마커 2개. 6초 |
| 3 | 모델 360° 회전 | 기립 자세. 8초 |
| 4 | 5cm 낙하 후 정착 | 측면 고정. 5초 |
| 5 | 관절 12개 순차 구동 | 다리별 shoulder→leg→foot. 10초 |
| 6 | 무게중심 어긋남 (−25.2 / −36.4 / 지지선 −25.0) | 6초 |

파일은 GitHub 이슈에 올리고 URL 만 여기 남길 것.

### BA-2. 링크별 질량 실측 결과 회신
**기한** 8/22 · `MASS` 에 반영 + **커밋 메시지에 실측/추정 구분**.
보고서 한계점의 "배분은 추정" 문장을 지울지 판단해야 함.

---

## 4. 로봇 켜지면 할 일 (A 유지)

| # | 결정 | 근거 |
|---|---|---|
| 20 | **무응답 시 기본값 도입.** 단 되돌리기 어려운 것은 무응답=보류 | 병목이 민호님(팀장·로봇·촬영·제출). 통제권을 지키려다 진행이 멈추는 손해가 더 큼 (A 제안, B 결정) |
| 19 | 조율 문서 **2개로 고정**: 계약(`업무분장`) + 큐(`결정`) | 계약은 안 바뀌고 큐는 시간 단위로 바뀜. 섞으면 매번 안 바뀌는 부분을 다시 읽음 |
| 14 | 8/23 을 영상 편집 전담일로 | 3분 영상은 구성·편집·자막·렌더로 1일. 양쪽 다 과소평가 |
| 13 | 정책 가중치는 저장소 `checkpoints/` 에 **최종 1개만** 커밋 | 제9조②3-가 는 HF 를 특정하지 않음. git 은 바이너리 영구 보존 |
| 12 | 링크별 질량 실측 P1→P2, 단 촬영 뒤 같은 세션에서 수행 | 서면평가에서 안 봄. 단 "무게중심 11.4mm 어긋남" 근거라 재는 것 자체는 가치 |
| 11 | 끝난 일은 **커밋 메시지로만** 보고. 별도 문서 금지 | 상대 에이전트가 작업 전 `git log` 를 읽음. 이미 그렇게 동작 중 |
| 10 | 인터페이스 질문은 **코드로** 답한다 | B 질문 10개 중 4개를 `rl/model_api.py` 로 답한 것이 가장 값싸고 오해 없었음 |
| 9 | 로봇 요청은 **모아서** 넘긴다 ("로봇 켜지면 할 일" 상시 유지) | 8/20 세션이 준비 부족으로 반쪽이 됨 |
| 8 | RL 실물 이식은 8/27 범위 밖, **11월 목표** | 제11조② — 2차 기능테스트(10/12~28) 시점 저장소 상태로 평가 |
| 7 | B 는 상수를 복제하지 않고 **`rl/model_api.py`** 만 참조 | 서보 오프셋·`standingTrunkHeight`(0.120 vs 0.262) 가 이미 어긋났음 |
| 6 | **MJCF 는 A 안 채택**, B 의 `oss_spotmicro/urdf/spot_micro.xml` 폐기 | A 안은 실측 상수 생성 + FK 대조 0.0000mm |
| 5 | `rl/` 는 **파일 단위**로 소유 분할. 디렉터리 재편 안 함 | D-7 에 import 경로 변경은 손해 |
| 4 | `.gitignore` 에 고아 미디어 2개 **경로 명시** | `git rm --cached` 만으로는 `git add -A` 에서 재유입 |
| 3 | 접수 정보 확정: **일반 / 자유과제(인공지능) / 팀장 손민호 / 3명** | 제출 기간 중 수정 가능 |
| 2 | 보고서는 **B 가 쓰고 A 가 검증** | 사실관계 오류는 근거와 함께 지적, 문장 반영은 B |
| 1 | 경계는 "누가 만드는가"가 아니라 **"어떤 사실을 누가 소유하는가"** | 같은 표가 두 곳에 있으면 반드시 어긋남 |

민호님이 CM4 를 켤 수 있을 때 통째로 실행. 순서 중요.

1. **현재 설정 그대로 1m 직진 측정 + 촬영** ← 안전핀. 튜닝 전에 먼저
2. 보행 튜닝 (주기 2800 / 스윙 0.143 / 발들어올림 30mm)
3. 하드웨어 라벨 확인 (서보 12개, 벅 모델명, 배터리 용량)
4. 링크별 질량 계량 (다리 1개 + 몸통)
5. 여유 시 회전 보행 촬영

---

## 5. 결정 로그

> 왜 그렇게 정했나의 기록. 작업 진행 상황은 쓰지 않는다.
