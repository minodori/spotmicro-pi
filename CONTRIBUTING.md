# 기여 안내

기여를 환영합니다. 이 문서는 규칙보다 **왜 그 규칙이 있는지**를 먼저 설명합니다.

---

## 이 저장소가 겪은 문제

이 로봇에는 조상이 여럿입니다. 기구 원설계는 김덕연(KDY0523), 소프트웨어는
FlorianWilk에서 출발해 한양대 Road Balance, robertchoi, orocapangyo를 거쳤고,
각 단계가 서로 다른 서보를 썼습니다. 자세한 계보는 [`NOTICE`](NOTICE) 2장에 있습니다.

문제는 **기구 치수가 코드 세 곳에 따로 적혀 있었다**는 것입니다. 한 곳만 고쳐도
저장소가 조용히 갈라집니다. 실제로 우리가 출발점으로 삼은 저장소는 한 프로세스
안에서 서로 다른 로봇 세 벌을 동시에 쓰고 있었습니다.

| 무엇이 읽는가 | 몸통 | 정강이 |
|---|---:|---:|
| 시뮬레이터가 읽는 URDF | 186 | 135 |
| 발끝 목표 계산 | 140 | — |
| 역기구학 | 140 | 100 |

**화면에 띄운 로봇과 관절을 명령하는 로봇이 달랐습니다.** 그리고 2020년 커밋 하나가
다리 길이를 바꿨는데 그 이유가 어디에도 없습니다. 물어볼 곳도 남지 않았습니다 —
원 커뮤니티의 GitLab 저장소는 공개 접근이 되지 않고 `spotmicro.org`는 도메인이
사라졌습니다.

그래서 이 저장소의 규칙은 전부 **"다음 사람이 물어보지 않아도 알 수 있게"** 로
수렴합니다.

---

## 규칙 1 — 기구 치수는 한 곳에서만 고칩니다

```
Kinematics/kinematics.py:59~65     ← 링크 길이의 유일한 정본
        │
        ├──→ rl/gen_mjcf.py        → rl/mjcf/*.xml  (시뮬레이션 모델, 생성물)
        ├──→ rl/model_api.py       → rl/envs/, rl/eval.py  (강화학습)
        └──→ RaspberryPi/          (실물 서보 제어)

  rl/validate_mjcf.py 가 이 셋의 일치를 8단계로 검증합니다
```

| 값 | 정본 | 직접 고치면 안 되는 곳 |
|---|---|---|
| 링크 길이 `l1 l2 l3 l4 L W` | `Kinematics/kinematics.py` | `rl/mjcf/*.xml`, URDF |
| 서보 영점·방향 | `Common/servo_map.py` | 제어 코드 안의 상수 |
| 보행 파라미터 | `Common/gait_params.py` | |
| 학습 전용 값 (보상 가중치 등) | `rl/envs/config.py` | |

> `rl/mjcf/*.xml` 은 **생성물입니다.** 손으로 고치면 다음 생성 때 사라지고, 그 사이에
> 시뮬레이션과 실물이 갈라집니다. 이 저장소가 겪은 문제가 정확히 그것입니다.

**`rl/envs/config.py` 에는 실물에 대응물이 있는 값을 두지 않습니다.** 보상 가중치처럼
시뮬레이션 안에만 존재하는 값만 둡니다. 이 프로젝트는 상수 중복으로 세 번 어긋났습니다
(링크 길이, 서보 영점, 기립 높이).

### 치수를 바꿨다면

```bash
uv run python rl/gen_mjcf.py        # 모델 재생성
uv run python rl/validate_mjcf.py   # 8/8 통과해야 합니다
```

기하 일치 게이트는 **0.0000 mm** 를 요구합니다. 반올림이 아니라 실제로 0이어야 합니다.

---

## 규칙 2 — 부품이 다르면 값을 재서 넣으십시오

이 저장소는 "받으면 바로 걷는다"고 약속하지 않습니다. **부품이 다른 것이 문제의
본질**이기 때문입니다. 대신 이렇게 약속합니다.

- **로봇이 없다면** — 클론하고 바로 시뮬레이션·검증·학습된 정책 재생까지 됩니다
- **로봇을 만든다면** — 자기 실측값을 `Kinematics/kinematics.py` 한 곳에 넣으면
  시뮬레이션 모델과 실물 제어가 함께 따라오고, 검증이 일치 여부를 알려줍니다

서보를 바꾸거나 프레임을 수정했다면 그것은 **버그가 아니라 정상적인 포크**입니다.
값을 재서 넣고 검증을 돌린 뒤, 무엇을 왜 바꿨는지 주석으로 남겨 주십시오.

### 치수 재는 법 — 함정이 있습니다

두 관절 축 사이 거리는 **3차원 직선거리가 아닙니다.** 두 축이 평행하므로 **축 방향
성분을 뺀** 거리여야 합니다. STL을 원피팅해서 나온 값을 그대로 쓰면 이 성분이 섞여
들어갑니다. 우리도 이 함정에 빠져 `l3` 를 125로 잘못 기록한 적이 있습니다
([`study/minho/work11.md`](study/minho/work11.md)).

---

## 규칙 3 — 커밋 메시지에 "왜"를 적습니다

이 저장소가 겪은 가장 큰 문제는 틀린 값이 아니라 **이유가 남지 않은 변경**이었습니다.
그러니 이것만은 지켜 주십시오.

> **무엇을 바꿨는지는 diff가 말합니다. 커밋 메시지는 왜 바꿨는지를 적는 자리입니다.**

특히 **기구 상수를 바꾼다면 근거를 반드시 남기십시오** — 무엇으로 어떻게 쟀는지,
다른 후보값이 있었다면 왜 그것을 택하지 않았는지.

좋은 예가 이미 저장소 안에 있습니다. [`Kinematics/kinematics.py:40~58`](Kinematics/kinematics.py)
은 `l3` 를 110으로 정한 근거로 Fusion 측정값, STL 원피팅값, 실물 줄자 실측을 모두
남기고, 그 변경이 몸통 높이 상한에 미치는 파급까지 적어 두었습니다.

---

## 개발 환경

```bash
git clone --depth 1 https://github.com/robertchoi/oss_spotmicro.git
cd oss_spotmicro
uv sync                             # Python 3.12, GPU 불필요
```

로봇 없이 실행할 수 있는 것:

```bash
uv run python rl/validate_mjcf.py   # 모델-실물 일치 8단계 검증
uv run python -m rl.eval --run checkpoints --command 0.2 0 0
```

로봇이 필요한 것은 `RaspberryPi/` 아래의 서보 제어 코드입니다.

---

## 이슈와 PR

- **이슈** — 무엇을 기대했고 무엇이 일어났는지, 그리고 **어떤 하드웨어인지**(서보 모델,
  링크 길이) 적어 주십시오. 이 프로젝트에서는 하드웨어 차이가 대부분의 차이를 만듭니다.
- **PR** — 기구 상수를 건드렸다면 `rl/validate_mjcf.py` 결과를 함께 붙여 주십시오.
  CI가 같은 검증을 자동으로 돌립니다.
- 큰 변경은 먼저 이슈로 논의해 주십시오.

---

## 라이선스

- 소프트웨어 — **GPL-3.0-or-later**
- 3D 모델(`STL/`, `STEP_Files/`, `Parts/`) — **CC BY 3.0**, 원저작자 KDY0523

**두 라이선스가 다릅니다.** 기여 전에 [`NOTICE`](NOTICE) 3장(상속/신규 코드 경계)과
4장(3D 모델 귀속)을 확인해 주십시오. 기여하시면 해당 라이선스로 배포되는 데 동의하는
것으로 간주합니다.
